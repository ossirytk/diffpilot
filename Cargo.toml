[package]
name = "diffpilot"
version = "0.1.0"
edition = "2021"

[lib]
name = "diffpilot_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.28", features = ["extension-module"] }

[profile.release]
lto = true
codegen-units = 1
strip = true
